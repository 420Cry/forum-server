# SSH private key for GitHub (used by install:clone).
SSH_PRIVATE_KEY=~/.ssh/id_rsa

# Directory containing forum-api and forum-app (siblings of forum-server).
PROJECTS_DIRECTORY=~/projects/forum

# GitHub org/owner for cloning. Repos: GITHUB_ORG/forum-api, GITHUB_ORG/forum-app
GITHUB_ORG=your-org

# Project folders to manage (must exist under PROJECTS_DIRECTORY).
PROJECTS="forum-api forum-app"
